### Завдання 1.
Код, який надали для першого завдання, представляє програму для числового інтегрування функції за допомогою як серійного, так і паралельного підходів з використанням OpenMP. Давайте розглянемо його по кусочках:

1. **Включення бібліотек та визначення констант:**
   ```c
   #include <math.h>
   #include <stdio.h>
   #include <omp.h>
   
   const double PI = 3.14159265358979323846;
   const double a = -4.0;
   const double b = 4.0;
   const int nsteps = 40000000;
   ```
   В цьому розділі включаються необхідні бібліотеки і визначаються константи, такі як \(\pi\), початок та кінець інтервалу і кількість кроків для інтегрування.

2. **Функція `func`:**
   ```c
   double func(double x) {
       return exp(-x * x);
   }
   ```
   Ця функція визначає функцію, яку ви хочете інтегрувати. У даному випадку це функція \(\exp(-x^2)\).

3. **Функція `integrate`:**
   ```c
   double integrate(double (*func)(double), double a, double b, int n) {
       double h = (b - a) / n;
       double sum = 0.0;
       for (int i = 0; i < n; i++)
           sum += func(a + h * (i + 0.5));
       sum *= h;
       return sum;
   }
   ```
   Ця функція виконує числове інтегрування заданої функції методом середнього арифметичного. Вона розділяє інтервал на n підінтервалів і обчислює суму за допомогою значень функції на середині кожного підінтервалу.

4. **Функція `integrate_omp`:**
   ```c
   double integrate_omp(double (*func)(double), double a, double b, int n) {
       // ... (паралельний код з використанням OpenMP)
   }
   ```
   Ця функція виконує паралельне числове інтегрування за допомогою OpenMP. Вона розділяє роботу між потоками для прискорення обчислень.

5. **Функція `run_serial`:**
   ```c
   double run_serial() {
       // ... (вимірює час виконання та виводить результат для серійного виконання)
   }
   ```
   Ця функція виконує інтегрування за допомогою серійного методу та вимірює час виконання.

6. **Функція `run_parallel`:**
   ```c
   double run_parallel() {
       // ... (вимірює час виконання та виводить результат для паралельного виконання)
   }
   ```
   Ця функція виконує інтегрування за допомогою паралельного методу та вимірює час виконання.

7. **Функція `main`:**
   ```c
   int main(int argc, char **argv) {
       // ... (головна функція, виводить інформацію, виконує обчислення та виводить час та швидкість)
   }
   ```
   Головна функція, яка керує виконанням програми. Вона виводить інформацію про задачу, виконує інтегрування, вимірює час виконання та виводить результати.

![task 1](https://media.discordapp.net/attachments/917547349864230912/1167176153673519124/image.png?ex=654d2c80&is=653ab780&hm=8f591a6cf7a35cb756b023aa76ab030b995a982087f1431e4e4328ecb78987b4&=&width=1382&height=275)

### Завдання 2. 

1. **Включення бібліотек:**
   ```c
   #include <stdio.h>
   #include <omp.h>
   #include <math.h>
   ```
   У цьому розділі включаються необхідні бібліотеки для роботи зі стандартним вводом/виводом, бібліотекою OpenMP для паралельних обчислень та математичними операціями.

2. **Визначення констант:**
   ```c
   const double k = 3.0;
   const int nsteps = 40000000;
   const double analytical_integral = (k / (k + 1)) * (pow(3, k + 1) - pow(-3, k + 1));
   ```
   В цьому розділі визначаються константи, такі як значення `k`, кількість кроків `nsteps` для числового інтегрування та обчислення аналітичного інтегралу на основі `k`.

3. **Функція `func`:**
   ```c
   double func(double x) {
       return k * pow(x, k);
   }
   ```
   Ця функція представляє функцію, яку ви хочете інтегрувати. У даному випадку це функція \(y(x) = k \cdot x^k\).

4. **Функція `integrate_trapezoidal`:**
   ```c
   double integrate_trapezoidal(double (*func)(double), double a, double b, int n) {
       // ... (код для числового інтегрування методом трапецій)
   }
   ```
   Ця функція використовує метод трапецій для числового інтегрування заданої функції на вказаному інтервалі.

5. **Функція `run_serial`:**
   ```c
   double run_serial() {
       // ... (код для виконання інтегрування та вимірювання часу виконання)
   }
   ```
   Ця функція виконує інтегрування заданої функції методом трапецій та вимірює час виконання обчислень.

6. **Функція `main`:**
   ```c
   int main(int argc, char** argv) {
       // ... (головна функція, виводить інформацію, запускає обчислення та виводить результати)
   }
   ```
   Головна функція керує виконанням програми. Вона виводить інформацію про завдання, виконує інтегрування, вимірює час виконання та виводить результати обчислень.

   Цей код виконує числове інтегрування функції \(y(x) = k \cdot x^k\) на інтервалі \([-3.00, 3.00]\) за допомогою методу трапецій. Він обчислює аналітичний інтеграл для порівняння та виводить результати обчислень для серійного виконання.

![task 1](https://media.discordapp.net/attachments/917547349864230912/1167178962116554782/image.png?ex=654d2f1d&is=653aba1d&hm=d785e8892ee3ec78aa7aa1dbf880cf294c87cff2a478e0521483dbf6e969d7be&=&width=1380&height=233)


### Завдання 3.
Цей код виконує паралельне числове інтегрування функції \(y(x) = k \cdot x^k\) за допомогою методу трапецій для різної кількості потоків та різних значень `nsteps`. Нижче наведено опис коду по кроках:

1. **Включення бібліотек:**
   ```c
   #include <stdio.h>
   #include <omp.h>
   #include <math.h>
   ```
   Включення необхідних бібліотек для стандартного вводу/виводу, OpenMP для паралельних обчислень і математичних операцій.

2. **Визначення констант:**
   ```c
   const double k = 3.0; 
   const int max_threads = 12; // Максимальна кількість потоків для тестування
   const int nsteps_values[] = { 40000000, 80000000 }; // Значення nsteps для тестування
   const double analytical_integral = (k / (k + 1)) * (pow(k, k + 1) - pow(-k, k + 1));
   ```
   В цьому розділі визначаються константи, такі як `k`, максимальна кількість потоків `max_threads` для тестування та масив `nsteps_values`, який містить різні значення `nsteps` для тестування. Також обчислюється аналітичний інтеграл для порівняння.

3. **Функція `func`:**
   ```c
   double func(double x) {
       return k * pow(x, k);
   }
   ```
   Ця функція представляє функцію \(y(x) = k \cdot x^k\), яку інтегрують.

4. **Функція `integrate_trapezoidal`:**
   ```c
   double integrate_trapezoidal(double (*func)(double), double a, double b, int n) {
       // ... (код для числового інтегрування методом трапецій)
   }
   ```
   Ця функція використовує метод трапецій для числового інтегрування функції на вказаному інтервалі.

5. **Функція `run_parallel`:**
   ```c
   double run_parallel(int num_threads, int nsteps) {
       // ... (встановлює кількість потоків та вимірює час виконання обчислень)
   }
   ```
   Ця функція встановлює кількість потоків за допомогою `omp_set_num_threads`, виконує інтегрування методом трапецій та вимірює час виконання.

6. **Функція `main`:**
   ```c
   int main(int argc, char** argv) {
       // ... (головна функція, виводить інформацію, виконує обчислення та виводить результати)
   }
   ```
   Головна функція керує виконанням програми. Вона виводить інформацію про кількість потоків та `nsteps`, виконує інтегрування для різної кількості потоків та різних значень `nsteps`, вимірює час виконання та виводить результати, включаючи швидкість виконання (speedup) порівняно з серійним виконанням.
![alt text](https://media.discordapp.net/attachments/917547349864230912/1167179412081475714/image.png?ex=654d2f88&is=653aba88&hm=22ea0dcfd2aab63d49d0e5ffd7e68049e7bd7372622f697b6571c509da422cab&=&width=1283&height=671)
**40000000**
![alt text](https://media.discordapp.net/attachments/917547349864230912/1167176020491784272/image.png?ex=654d2c60&is=653ab760&hm=7e9f1733bc3114a0a09b27ad35ea576a6dc8d59a78bbef31144118ad46af1070&=&width=1142&height=671)
**80000000**
![alt text](https://media.discordapp.net/attachments/917547349864230912/1167176020491784272/image.png?ex=654d2c60&is=653ab760&hm=7e9f1733bc3114a0a09b27ad35ea576a6dc8d59a78bbef31144118ad46af1070&=&width=1142&height=671)

